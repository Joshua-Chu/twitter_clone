extends layout/login-layout.pug
block content
    .loginContainer
        h1 Login
        form#form(method="POST") 
            p#errorMessage #{errorMessage}
            //- input#userName(type="text", name="logUsername", placeholder="Username/Email", required="")
            //- input#password(type="password", name="logPassword", placeholder="password", required="")

            input#userName(type="text", name="logUsername", placeholder="Username/Email", value="chaleahChu" required="")
            input#password(type="password", name="logPassword", placeholder="password",value="1234", required="")
            input(type="submit", value="Login") 
        a(href="/register") Need an account? Register here.

        script. 
            const errorMessage = document.getElementById("errorMessage")

            setTimeout(() => {
                errorMessage.textContent = ""
            }, 2000)

